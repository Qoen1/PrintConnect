@using PrintConnect.Domain.Entities
@implements IDisposable

@inject NavigationManager NavigationManager

<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">PrintConnect.Server</a>
    </div>
</div>

<input type="checkbox" title="Navigation menu" class="navbar-toggler"/>

<div class="nav-scrollable" onclick="document.querySelector('.navbar-toggler').click()">
    <nav class="nav flex-column">
        @* <AuthorizeView> *@
            @* <Authorized> *@
                <div class="nav-item px-3">
                    <div class="nav-link">
                        <span class="bi bi-lock-nav-menu" aria-hidden="true"></span> printers:
                    </div>
                    <hr/>
                    <div style="overflow: scroll;">
                        @foreach (var printer in Printers)
                        {

                            <div class="nav-item px-3">
                                <NavLink class="nav-link" href="@($"/printer/{printer.Id}")">
                                    @printer.Name
                                </NavLink>
                            </div>
                        }
                    </div>
                </div>
            @* </Authorized> *@
        @*     <NotAuthorized> *@
        @*         <div class="nav-item px-3"> *@
        @*             <NavLink class="nav-link" href="Account/Register"> *@
        @*                 <span class="bi bi-person-nav-menu" aria-hidden="true"></span> Register *@
        @*             </NavLink> *@
        @*         </div> *@
        @*         <div class="nav-item px-3"> *@
        @*             <NavLink class="nav-link" href="Account/Login"> *@
        @*                 <span class="bi bi-person-badge-nav-menu" aria-hidden="true"></span> Login *@
        @*             </NavLink> *@
        @*         </div> *@
        @*     </NotAuthorized> *@
        @* </AuthorizeView> *@
    </nav>
</div>

@code {
    private string? currentUrl;
    public List<Printer> Printers = [
        Printer.Create("http://192.168.178.158/", "maker", "WpZhQTj86AKwX4z", "ger-de-printer", "je moeder", "prusa mk4s")
    ];

    protected override void OnInitialized()
    {
        currentUrl = NavigationManager.ToBaseRelativePath(NavigationManager.Uri);
        NavigationManager.LocationChanged += OnLocationChanged;
    }

    private void OnLocationChanged(object? sender, LocationChangedEventArgs e)
    {
        currentUrl = NavigationManager.ToBaseRelativePath(e.Location);
        StateHasChanged();
    }

    public void Dispose()
    {
        NavigationManager.LocationChanged -= OnLocationChanged;
    }

}